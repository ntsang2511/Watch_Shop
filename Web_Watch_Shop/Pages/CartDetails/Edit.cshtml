@page
@model Web_Watch_Shop.Pages.CartDetails.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Cart_Detail</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Cart_Detail.Cart_DetailID" />
            <div class="form-group">
                <label asp-for="Cart_Detail.CartID" class="control-label"></label>
                <select asp-for="Cart_Detail.CartID" class="form-control" asp-items="ViewBag.CartID"></select>
                <span asp-validation-for="Cart_Detail.CartID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
